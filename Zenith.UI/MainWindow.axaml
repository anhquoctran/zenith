<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:data="using:Zenith.Data"
        xmlns:core="using:Zenith.Core"
        x:Class="Zenith.UI.MainWindow"
        Title="Zenith Screen Recorder"
        Width="900" Height="600"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        TransparencyLevelHint="Mica"
        ExtendClientAreaToDecorationsHint="True">
    
    <Window.Styles>
        <Style Selector="Button.accent">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
        <Style Selector="Button.accent:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#0098FF"/>
        </Style>
    </Window.Styles>

    <Grid ColumnDefinitions="350,*" Margin="0,30,0,0">
        <!-- Left Pane: Configuration & Audio -->
        <Border Grid.Column="0" Background="#252526" BorderBrush="#333333" BorderThickness="0,0,1,0" Padding="20">
            <StackPanel Spacing="15">
                <TextBlock Text="Source Selection" FontSize="20" FontWeight="Bold" Foreground="#ECECEC"/>
                
                <TextBlock Text="Video Source" FontWeight="SemiBold" Foreground="#AAAAAA" Margin="0,10,0,0"/>
                <ComboBox x:Name="VideoSourceComboBox" SelectedIndex="0" HorizontalAlignment="Stretch" SelectionChanged="VideoSourceComboBox_SelectionChanged">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="core:VideoSource">
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
                <TextBlock Text="Camera" FontWeight="SemiBold" Foreground="#AAAAAA" Margin="0,10,0,0"/>
                <ComboBox x:Name="CameraComboBox" SelectedIndex="0" HorizontalAlignment="Stretch">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="core:WebcamSource">
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="Audio Source" FontWeight="SemiBold" Foreground="#AAAAAA" Margin="0,10,0,0"/>
                <ComboBox x:Name="AudioSourceComboBox" SelectedIndex="0" HorizontalAlignment="Stretch">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="core:AudioSource">
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="Audio Waveform" FontWeight="SemiBold" Foreground="#AAAAAA" Margin="0,10,0,0"/>
                <Border Background="#1A1A1A" CornerRadius="8" Height="100">
                    <Canvas x:Name="WaveformCanvas" ClipToBounds="True">
                        <!-- Waveform drawn here via code-behind -->
                    </Canvas>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="20" Margin="0,20,0,0">
                    <Button x:Name="RecordButton" Content="●" Click="RecordButton_Click" Background="#FF4444" Foreground="White" Width="48" Height="48" CornerRadius="24" FontSize="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" ToolTip.Tip="Record"/>
                    <Button x:Name="StopButton" Content="⏹" Click="StopButton_Click" Background="#007ACC" Foreground="White" Width="48" Height="48" CornerRadius="24" FontSize="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" IsEnabled="False" ToolTip.Tip="Stop"/>
                    <Button Content="⧉" Click="ShowWidget_Click" Background="#007ACC" Foreground="White" Width="48" Height="48" CornerRadius="24" FontSize="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" ToolTip.Tip="Widget"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Right Pane: Preview & History -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Preview" FontSize="20" FontWeight="Bold" Foreground="#ECECEC" Margin="0,0,0,10"/>
            
            <Border Grid.Row="1" Background="#252526" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Height="250" Margin="0,0,0,20" ClipToBounds="True">
                <Image x:Name="PreviewImage" Stretch="Uniform" />
            </Border>

            <TextBlock Grid.Row="2" Text="Recording History" FontSize="20" FontWeight="Bold" Foreground="#ECECEC" Margin="0,0,0,15"/>
            
            <ListBox x:Name="HistoryListBox" Grid.Row="3" Background="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="data:Record">
                        <Border Background="#2D2D30" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <Border Grid.Column="0" Width="100" Height="56" Background="#111111" CornerRadius="4"/>
                                <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" FontSize="16" Foreground="#ECECEC"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="{Binding Resolution}" Foreground="#AAAAAA" FontSize="12"/>
                                        <TextBlock Text=" • " Foreground="#AAAAAA" FontSize="12"/>
                                        <TextBlock Text="{Binding Codec}" Foreground="#AAAAAA" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{Binding Duration, StringFormat={}{0:hh\\:mm\\:ss}}" Foreground="#ECECEC" VerticalAlignment="Center" FontSize="14" FontWeight="Medium"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
